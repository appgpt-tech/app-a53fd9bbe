//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const SparePartsTitle = () => {
  const record = useRecordContext();
  return <span>SpareParts {record ? `"${record.name}"` : ''}</span>;
};

export const SparePartsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="name" />
      <NumberField source="price" />
      <TextField source="compatibility" />
      <NumberField source="stockQuantity" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const SparePartsEdit = () => (
  <Edit title={<SparePartsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="name" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="price" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="compatibility" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="stockQuantity" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const SparePartsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="name" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="price" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="compatibility" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="stockQuantity" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
  ,
];
